# The dashboard runs entirely from client-side sample data,
# so no database is required for the public deployment.
# Leave this file empty on Vercel / Netlify unless you connect a real database later.
DATABASE_URL=
